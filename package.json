{
  "name": "zetachain-universal-nft-bridge",
  "version": "1.0.0",
  "description": "Cross-chain NFT marketplace on ZetaChain",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network zeta_testnet",
    "test": "hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@zetachain/protocol-contracts": "^8.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.3.1"
  }
}
